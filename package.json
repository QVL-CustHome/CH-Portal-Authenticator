{
  "name": "ch-portail-authenticator",
  "version": "0.1.0",
  "private": true,
  "description": "Portail d'authentification CustHome - front React servi par Node.js, derriere CH-Api-GateWay",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "lint": "npm run lint -w client && npm run lint -w server"
  },
  "engines": {
    "node": ">=20"
  }
}
